version: "3"
services:
  client:
    build: ./client
    ports:
      - "80:80"
    depends_on:
      - backend
  backend:
    build: ./backend
    ports:
      - "8080:8080"
  nginx:
    restart: always
    build: ./nginx
